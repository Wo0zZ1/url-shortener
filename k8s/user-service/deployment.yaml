apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-c
  namespace: url-shortener
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-c
  template:
    metadata:
      labels:
        app: service-c
    spec:
      containers:
        - name: service-c
          image: ghcr.io/username/service-c:latest
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_HOST
              value: service-c-postgres
            - name: DATABASE_PORT
              value: '5432'
            - name: DATABASE_USER
              value: user
            - name: DATABASE_PASSWORD
              value: password
            - name: DATABASE_NAME
              value: service_a_db
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
